import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // experimental: {
  //   nodeMiddleware: true, // enable Node.js middleware support (experimental in some Next versions)
  // },
};

export default nextConfig;
